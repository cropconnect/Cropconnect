[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["cd cropconnect-backend && pip install -r requirements.txt"]

[start]
cmd = "cd cropconnect-backend && python migrate_db.py && uvicorn app:app --host 0.0.0.0 --port ${PORT:-8001}"
