{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "python migrate_db.py && uvicorn app:app --host 0.0.0.0 --port ${PORT:-8001}",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 30
  }
}
